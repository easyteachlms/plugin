const QuizResults = (obj) => {
    console.log(obj);
    return (
        <div>
            This is a custom result page. You can use obj to render your custom
            result page
        </div>
    );
};

export default QuizResults;
